#include<iostream>
#include<vector>
using namespace std;

int getDistance(vector<int> & arr, int N, int C){
    
}

int main() {

    vector<int> arr = {1, 2, 8, 4 ,9};
    int N = arr.size(), C = 3; 

    cout<<getDistance(arr, N, C)<<endl;
    return 0;
}